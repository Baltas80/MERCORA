const button=document.querySelector("#openDispute"),message=document.querySelector("#message");function csrf(){const m=document.cookie.match(/(?:^|; )mercora_csrf=([^;]+)/);return m?decodeURIComponent(m[1]):""}async function api(path,options={}){const r=await fetch("/api"+path,{credentials:"same-origin",...options,headers:{"Content-Type":"application/json",...(options.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||d.detail||"request_failed");return d}button.addEventListener("click",async()=>{const qs=new URLSearchParams(location.search),order_id=qs.get("order_id"),seller_account_id=qs.get("seller"),description=document.querySelector("#description").value.trim();if(!order_id||!seller_account_id){message.textContent="Faltan parámetros del pedido.";return}if(description.length<10){message.textContent="Describe el problema con suficiente detalle.";return}try{const d=await api("/disputes",{method:"POST",headers:{"X-CSRF-Token":csrf()},body:JSON.stringify({order_id,seller_account_id,reason_code:document.querySelector("#reason").value,description})});document.querySelector("#disputeStatus").textContent=d.status;message.textContent="Disputa abierta: "+d.id}catch(e){message.textContent=e.message}});
